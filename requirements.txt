# Torch wheels: primary index is PyTorch cu121 only (no PyPI), so pip cannot pick cu130 builds.
--index-url https://download.pytorch.org/whl/cu121
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# Remaining packages are not hosted on download.pytorch.org; use PyPI as primary index below.
--index-url https://pypi.org/simple
opencv-python>=4.9.0.80
diffusers>=0.31.0
transformers>=4.49.0
tokenizers>=0.20.3
accelerate>=1.1.1
tqdm
imageio
easydict
ftfy
dashscope
imageio-ffmpeg
gradio>=5.0.0
numpy>=1.23.5,<2
xfuser>=0.4.1
pyloudnorm
soundfile>=0.12.1
librosa>=0.10.0
einops>=0.8.0
huggingface-hub>=0.19.0
xformers>=0.0.28
ninja
packaging
safetensors>=0.3.1
